package mastering.jdbc.dao1;

import java.sql.PreparedStatement;
import java.sql.SQLException;
import java.util.List;
import java.util.logging.Logger;



/*
public class DaoUtils {
	private static final Logger log = Logger.getLogger(DaoUtils.class.getName());
	public static <T> T rethrowAsRuntime(UnsafeSupplier<T> supplier) {
		return supplier.get();
	}
	
	public static void logPreparedStatement(final boolean isSilent, PreparedStatement preparedStatement) {
		if (!isSilent ) {
			log.info(preparedStatement.toString());
		}
	}
	
	private static void setupParameters(PreparedStatement preparedStatement, List<Object> params) throws SQLException {
		setValues(params, preparedStatement, 1);
	}
}
*/
